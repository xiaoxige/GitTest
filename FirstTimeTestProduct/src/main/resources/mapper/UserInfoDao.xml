<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.xiaoxige.firsttimetestprodect.dao.UserInfoDao">

    <insert id="registerUser">

        insert into userInfo (userName, userPwd, nickName)
        VALUES ((#{user.userName}), (#{user.userPwd}), (#{user.nickName}))

    </insert>

</mapper>
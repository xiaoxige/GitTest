<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xiaoxige.firstssm.dao.UserTestDao">
    <insert id="addUserInfo">

        insert into Users(userName, userPwd) VALUES ((#{user.userName}), (#{user.userPwd}))

    </insert>
</mapper>
